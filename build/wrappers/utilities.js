const getUtilities = function(globalSettings, state, controllers, globalUtilities) {
    const appUtility = ()=>{}/* replace me */;
    const scopedUtility = appUtility.bind(this);
    return scopedUtility;
};

export default getUtilities;